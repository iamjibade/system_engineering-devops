#!/usr/bin/env bash
# A Bash script that sends a SIGTERM to the 7-highlander process

# Find the PID(s) of 7-highlander
pids=$(pgrep -f "7-highlander")

# Send SIGTERM to each PID
for pid in $pids; do
    kill -TERM "$pid"
done
